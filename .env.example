# Copy this file to .env (do not commit .env)
# Base URL for the Demo Web Shop site
BASE_URL=https://demowebshop.tricentis.com

# Optional: use an existing account (tests can still run as guest)
USER_EMAIL= 
USER_PASSWORD=

# Optional: set to 1 to force headed mode (overrides config defaults)
HEADED=